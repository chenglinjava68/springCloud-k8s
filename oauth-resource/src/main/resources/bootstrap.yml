spring:
  profiles:
    active: test
  application:
    name: oauthResource
  cloud:
    kubernetes:
      config:
        sources:
        - namespace: default
          name: oauthResource
      reload:
        mode: event
        enabled: true