spring:
  profiles:
    active: test
  application:
    name: feign-call
  cloud:
    kubernetes:
      config:
        sources:
        - namespace: default
          name: feign-call
      reload:
        mode: event
        enabled: true
        strategy: refresh