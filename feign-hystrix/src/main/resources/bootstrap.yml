spring:
  profiles:
    active: test
  application:
    name: feign-service
  cloud:
    kubernetes:
      config:
        sources:
        - namespace: default
          name: ${spring.application.name}
      reload:
        mode: event
        enabled: true