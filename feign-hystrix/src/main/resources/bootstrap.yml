spring:
  profiles:
    active: test
  cloud:
    kubernetes:
      config:
        sources:
        - namespace: default
          name: ${spring.application.name}
      reload:
        mode: event
        enabled: true