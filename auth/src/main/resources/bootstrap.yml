spring:
  profiles:
    active: test
  application:
    name: oauth
  cloud:
    kubernetes:
      name: oauth
      namespace: default
      config:
        sources:
        - namespace: default
          name: oauth