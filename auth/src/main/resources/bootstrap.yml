spring:
  profiles:
    active: test
  application:
    name: oauth
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      username: config
      password: cf123456
      profile: ${spring.profiles.active}
      label: master
      fail-fast: true
    bus:
      trace:
        enabled: true #配置变化跟踪