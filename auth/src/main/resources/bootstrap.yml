spring:
  profiles:
    active: test
  application:
    name: oauth
  cloud:
    kubernetes:
      config:
        sources:
        - namespace: default
          name: oauth
      reload:
        mode: event
        enabled: true